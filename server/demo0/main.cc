#include "listen.h"
#include "SystemData.h"
#include <string>
#include <iostream>
using namespace std;
int main()
{
    Listen lis;
//    SystemData sys;
//    sys.connectMysql("localhost","root","Z001221z","user");
//     string sql = "select password from user_info where(id=100001);";
//    cout<<sys.query(sql);
    return 0;
}